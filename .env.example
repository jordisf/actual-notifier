# Plantilla de configuracion. Copia este fichero a .env y rellena los valores reales.

# Conexion a Actual Budget
ACTUAL_SERVER_URL=http://actual-budget:5006
ACTUAL_PASSWORD=tu_password_de_actual
ACTUAL_SYNC_ID=id_del_presupuesto_a_sincronizar

# Servidor SMTP para el envio de correos
SMTP_HOST=smtp.tu-proveedor.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=usuario@tu-dominio.com
SMTP_PASS=tu_password_smtp

# Destinatarios del reporte (separados por comas)
NOTIFICATION_EMAIL=destino1@ejemplo.com,destino2@ejemplo.com

# --- Telegram (optional: empty token = email-only mode) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_GROUP_ID=
TELEGRAM_MAX_TX_MESSAGES=15
# Closed list of category NAMES offered as buttons (comma separated).
# Empty = offer every non-income category (legacy). Names not present in
# the budget are skipped with a warning. Use:
#   docker exec actual_notifier_dev node /app/src/dev/list-categories.js
# to list this budget's available category names.
TELEGRAM_CATEGORIES=
TELEGRAM_POLL_TIMEOUT=30
TELEGRAM_DRY_RUN=true
DATA_DIR=/app/data
# NOTE: changing TELEGRAM_BOT_TOKEN/TELEGRAM_GROUP_ID requires `docker compose restart notifier-listener`

# --- Web configuration panel (specs/001-config-webapp) ---
# Host interface the panel's port is bound to. Leave unset (or 127.0.0.1) to
# keep it localhost-only. Set to your LAN/VPN-facing address to reach it from
# another machine, e.g.:
# PANEL_BIND_HOST=192.168.1.10
PANEL_BIND_HOST=127.0.0.1
PANEL_PORT=8080

